import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    backgroundColor: 'lightgray',
    borderRadius: 6,
    height: 50,
    width: '100%',
    fontSize: 16,
    paddingLeft: 10,
  },
  activityIndicator: {
    marginTop: 20,
  },
});
